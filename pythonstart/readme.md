# Python spezielle Kommandos

Installation boto3

`pip install boto3`

Aktuelle Packete sichern

`pip freeze > requirements.txt`

Alle Packete installieren

`pip install -r /path/to/requirements.txt`
